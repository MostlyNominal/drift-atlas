# Node
node_modules/
dist/
.vite/
*.local
tsconfig.tsbuildinfo

# Python (data processing)
__pycache__/
.venv/
*.pyc

# OS
.DS_Store
Thumbs.db

# Processed data is REGENERATED by CI from data/raw and committed by the
# workflow into public/data/processed. Locally generated copies are ignored
# to avoid noisy diffs; CI is the source of truth.
# (Comment the next line out if you prefer to commit processed data manually.)
# public/data/processed/

# Raw FT4 binaries and large SEM images may use Git LFS (see .gitattributes).
